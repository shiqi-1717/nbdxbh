# 基础工具
pip==24.0
setuptools<71
wheel

# Streamlit 与兼容依赖
streamlit==1.36.0
rich==13.9.4          # 与 streamlit 1.36 兼容

# 科学计算
numpy==1.26.4
pandas==2.2.3
scipy==1.11.4         # 给 scikit-fuzzy 用，避免源码编译
pillow==10.4.0
XlsxWriter==3.2.0
requests==2.32.3
websocket-client==1.8.0
scikit-fuzzy==0.4.2

# OpenCV：只装无界面版，避免 libGL 依赖
opencv-python-headless==4.8.1.78

# PyTorch（CPU 版）
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# YOLO
ultralytics==8.2.60
